/**
 * Re-export shared components
 * 
 * Components will be added here as we build them.
 * This provides a clean import path: import { Button, Card } from '$lib/components'
 * 
 * Future components to add:
 * - Button (primary, secondary, danger variants)
 * - Card
 * - Modal
 * - Badge
 * - FormInput
 * - LoadingSpinner
 * - EmptyState
 * - Pagination
 */

// Placeholder export to prevent empty module errors
export {};